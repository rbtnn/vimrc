
[include]
    path = ~/.gitconfig.local

[user]
  name = rbtnn
  email = naru123456789@gmail.com

[http]
    sslVerify = false

[core]
    autocrlf = false
    quotepath = false
    editor = vim
    excludesfile = ~/.gitignore.global

[alias]
    save=stash save
    pop=stash pop
    br=branch
    ci=commit
    co=checkout
    ls=ls-files
    lg=log --pretty=oneline --graph --abbrev-commit
    po=push -u origin
    st=status
    subpull=submodule foreach git pull origin master

[color]
    diff = true
    status = true
    branch = true
    interactive = false

[help]
    format = man

[credential]
    helper = wincred

;  vim: set ft=gitconfig ff=unix :
